name: Deploy pulsescope

on:
  push:
    branches: [main]

env:
  IMAGE_NAME: ghcr.io/wikolabs/pulsescope

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/wikolabs/pulsescope:latest

      - name: Deploy to VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.WIKOLABS_VM_HOST }}
          username: ${{ secrets.WIKOLABS_VM_USER }}
          key: ${{ secrets.WIKOLABS_VM_SSH_KEY }}
          script: |
            docker pull ghcr.io/wikolabs/pulsescope:latest
            docker stop pulsescope 2>/dev/null || true
            docker rm pulsescope 2>/dev/null || true
            docker run -d               --name pulsescope               -p 3001:3000               --restart unless-stopped               ghcr.io/wikolabs/pulsescope:latest
            echo "Deployed pulsescope on port 3001"
